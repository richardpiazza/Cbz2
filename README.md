# Cbz2

This is a Swift package manager module for bzip2. You can install bzip2 with brew (`brew install bzip2`).

To use, add as a package dependency:

    .package(url: "https://github.com/richardpiazza/Cbz2.git", from: "1.0.0")
